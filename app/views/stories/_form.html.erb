<%= form_for @story do |f| %>
<%= f.hidden_field :question_id, value: @story.questions.last.id %>
  <%= f.text_area :body, cols: 60, rows: 10, placeholder: "Tell a story that helps to answer this question" %> <br>
  <%= f.submit %>
<% end %>
